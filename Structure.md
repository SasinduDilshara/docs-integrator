small description on what are the use cases of the artifact type. Overall summary of the sub sections covered in the page.

If user need a third part broker/service we can assume that user have already configured it.

Sections should be in below order
1. Creating Artifact type
	- Start with step to create an integration
	- Explain all form fields
	- End with Creating an empty service

2. Service Configuration
	- Start with via to find the service configuration
	- Explain all the service configuration fields

3. Listener Configuration
	- Start with via to find Listener Configuration
	- If service can attach multiple listeners explain the use case
	- Explain all the Listener configuration fields

4. Defining Resources/Handlers
	- Explain path and use case of handler 
	- Need to explain all the fields in resource/handler forms with thier use case

5. Error Handling 

Each sub section mentioned above should have both visual designer and ballerina code section if visual desginer support exist. 

If changing listener/service configurations leads to common use cases related to the service move those specific use case into a single page. This can be done for resources/handlers as well.

When showing configurations show all the field and their descriptions.

Whats next section should link to relavent Tutorials, Data Mapper section and Connectors section. 
